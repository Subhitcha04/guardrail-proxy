# Pinned explicitly, above spacy/presidio. Without this pin, pip's resolver
# picks numpy>=2.0 for sentence-transformers' generic "numpy" requirement,
# which is binary-incompatible with thinc 8.2.5's compiled extension (built
# against the pre-2.0 numpy dtype struct layout). That combination crashes
# on import with "numpy.dtype size changed, may indicate binary
# incompatibility" — hit this for real while building this. scipy (pulled in
# transitively by sentence-transformers -> scikit-learn) will resolve to an
# older, numpy<2.0-compatible version automatically; don't pin scipy yourself.
numpy==1.26.4
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
PyYAML==6.0.2
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.6
sentence-transformers==3.1.1
asyncpg==0.29.0
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
